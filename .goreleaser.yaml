# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj
version: 2

before:
  hooks:
    - go mod tidy
    - go generate ./...
    - go install github.com/tc-hib/go-winres@latest
    - go-winres make --in cmd/aetherclaw-launcher/winres/winres.json --out cmd/aetherclaw-launcher/rsrc --product-version={{ .Version }} --file-version={{ .Version }}

builds:
  - id: aetherclaw
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
      - -X github.com/AetherClawTech/aetherclaw/cmd/aetherclaw/internal.version={{ .Version }}
      - -X github.com/AetherClawTech/aetherclaw/cmd/aetherclaw/internal.gitCommit={{ .ShortCommit }}
      - -X github.com/AetherClawTech/aetherclaw/cmd/aetherclaw/internal.buildTime={{ .Date }}
      - -X github.com/AetherClawTech/aetherclaw/cmd/aetherclaw/internal.goVersion={{ .Env.GOVERSION }}
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/aetherclaw
    ignore:
      - goos: windows
        goarch: arm

  - id: aetherclaw-launcher
    binary: aetherclaw-launcher
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/aetherclaw-launcher
    ignore:
      - goos: windows
        goarch: arm

  - id: aetherclaw-launcher-tui
    binary: aetherclaw-launcher-tui
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/aetherclaw-launcher-tui
    ignore:
      - goos: windows
        goarch: arm

dockers_v2:
  - id: aetherclaw
    dockerfile: docker/Dockerfile.goreleaser
    extra_files:
      - docker/entrypoint.sh
    ids:
      - aetherclaw
    images:
      - "ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER }}/aetherclaw"
    tags:
      - "{{ .Tag }}"
      - "latest"
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/riscv64

archives:
  - formats: [tar.gz]
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: [zip]

nfpms:
  - id: aetherclaw
    builds:
      - aetherclaw
      - aetherclaw-launcher
      - aetherclaw-launcher-tui
    package_name: aetherclaw
    file_name_template: >-
      {{ .PackageName }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "arm64" }}aarch64
      {{- else if eq .Arch "arm" }}armv{{ .Arm }}
      {{- else }}{{ .Arch }}{{ end }}
    vendor: aetherclaw
    homepage: https://github.com/{{ .Env.GITHUB_REPOSITORY_OWNER }}/aetherclaw
    maintainer: aetherclaw contributors
    description: aetherclaw - a tool for managing and running tasks
    license: MIT
    formats:
      - rpm
      - deb
    bindir: /usr/bin

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

# upx:
#    - enabled: true
#      compress: best
#      lzma: true

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
